# Dev-only broker config (plaintext, anonymous). NEVER use in production —
# prod uses EMQX Serverless (MQTTS 8883, auth required). See .env.example.
listener 1883
allow_anonymous true
persistence false
